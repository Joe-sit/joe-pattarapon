# อินโทรของ /2026

เอกสารนี้อธิบายว่าอินโทรทำงานยังไง ทำไมถึงเลือกทำแบบนี้ และมีกับดักอะไรบ้าง
ที่เคยเดินชนมาแล้ว — อ่านก่อนแก้ ไม่งั้นจะแก้แล้วพังซ้ำที่เดิม

## ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | หน้าที่ |
| --- | --- |
| `src/components/IntroSequence.tsx` | ไทม์ไลน์ทั้งหมด + องก์แรก (วงโคจร) ที่เป็น DOM |
| `src/components/AsciiField.tsx` | แคนวาสอักขระ — พื้นหลัง ภูมิประเทศ และโลโก้ |
| `src/components/IntroDebug.tsx` | แผงดีบัก เห็นเฉพาะ `import.meta.env.DEV` |
| `src/components/introSequence.css` | รูปลักษณ์ขององก์แรกกับแผงดีบัก |
| `src/components/JoeLettersSplash.tsx` | ต้นทางของพาธโลโก้และไทม์ไลน์ประกอบร่าง |

คลิปอ้างอิงอยู่ที่ `~/Downloads/intro-frame/` แตกไว้ 189 เฟรม

---

## หนึ่ง: คลิปอ้างอิง

189 เฟรมที่ 30fps = 6.3 วินาที แต่ **มีเฟรมซ้ำเป็นคู่** (f003=f004, f007=f008, …)
ภาพจริงเดินที่ 15fps ราว 95 ภาพ เวลาวัดอัตราการเคลื่อนที่ข้ามเฟรมต้องหารสอง

ลำดับของคลิป:

| เฟรม | สิ่งที่เห็น |
| --- | --- |
| f001-f012 | แผ่นโค้ด เลข 20/24 ปื้นฮาล์ฟโทน กวาดตามเข็มแล้วล้นออกนอกเฟรม |
| f013-f041 | ของเล็กเกาะเป็นวง รัศมีนิ่งราว 180-200px หมุนช้าลงเรื่อย ๆ |
| f042-f051 | วงหดเร็วมากจนยุบ |
| f047-f053 | ไทล์กลางจอ: ชิปฟ้า+`1` → ชิปขาว+`2` → ชิปฟ้า+`%` |
| f035-f070 | เกาะ ASCII สีฟ้าโผล่จากขอบจอ (ก่อน f035 จอดำสนิท) |
| f070-f112 | หัวเดียววิ่งลากเส้น เกิดที่มุมบนซ้าย ทิ้งรอยไว้ข้างหลัง สีส้มโผล่ที่ f082 |
| f112-f132 | บรรจบครบวงแล้วถมทึบ |
| f132-f176 | หมุน + ปื้นขาวไล่เฉดกวาด |
| f183 | พื้นหลังดับสนิท เหลือรูปบนดำล้วน |
| f189 | หั่นเป็นขีดนอนเรียงเป็นวง วงเล็กกว่า f183 ราวครึ่ง |

---

## สอง: โมเดลหลัก — สนามความสูงใบเดียว

หัวใจของทั้งอินโทรคือ **ไม่มีเลเยอร์** พื้นหลังกับตัวโลโก้เป็นค่าเดียวกัน
ต่างกันแค่ "สูงแค่ไหน" แล้วถูกแปลงเป็นอักขระด้วยบันไดเดียวกันทั้งจอ

```
_  •  =  +  <  :  ;  !  $  @  #  ▉
└── ฟ้าเข้ม ──┘└ ฟ้า ┘└ คราม ┘└ ส้ม ┘└ บล็อกทึบ
```

ค่าต่อช่องคือ `level = max(ภูมิประเทศ, โลโก้)` แล้วส่งเข้า ramp เดียว
"การแปลงร่าง" จึงเกิดเองเมื่อโลโก้ไต่ระดับขึ้นมาแซงภูมิประเทศ ไม่ต้องมีรอยต่อ

ระดับของโลโก้:

```
level = r × heat × (0.70 + 0.24·fill + 0.28·solid)
```

- `r` = ความทึบของรูป + คอนทัวร์ที่เบลอออกไป (สูงสุด 1.25)
- `heat = ink^0.35` — ไต่บันไดเร็วกว่าเชิงเส้น
- ค่าฐาน `0.70` ทำให้ส่วนที่มีรูปแล้วอยู่ที่ชั้น `@` (**ตัวอักษรสีส้ม**) ยังไม่ใช่บล็อก
- `fill` กับ `solid` เท่านั้นที่ดันข้ามไปเป็นบล็อกทึบ

**คอนทัวร์รอบรูปคือสำเนาที่เบลอของตัวทึบ** ค่าที่ฟุ้งออกไปตกกลางบันได จึงถูก
เรนเดอร์เป็น `# @ $ ! ; : < + =` ไล่ออกไปเป็นวง ๆ เหมือนคอนทัวร์ของเกาะ
รัศมีเบลอผูกกับ `solid` — หนา 4-6 ช่องตอนยังเป็นตัวอักษร แล้วหดเหลือ 1 ช่องตอนทึบ

---

## สาม: สัญญาของ `AsciiDrive`

`IntroSequence` เขียนค่าลง ref ทุกเฟรม ไม่ผ่าน state (ไม่งั้นรีเรนเดอร์ 24 ครั้ง/วินาที)

| ฟิลด์ | ความหมาย |
| --- | --- |
| `gain` | ความสว่างรวมของภูมิประเทศ |
| `guide` | ดันแกนเกาะขึ้นถึงชั้นคราม (คูณเฉพาะค่าที่รอดจากการตัดแล้ว) |
| `ink` | ดันโลโก้ขึ้นบันได |
| `build` | ความคืบหน้าของการประกอบร่าง 0-1 |
| `solid` | เก็บกวาดตอนท้าย + หรี่เส้นขอบและคอนทัวร์ |
| `spin` | ส่ายเบา ๆ `sin(spin) × 0.30` กลับมาแบนราบตอน `2π` |
| `sweep` | ตำแหน่งปื้นขาว ติดลบ = ยังไม่ถึงคิว |
| `shatter` | หั่นเป็นขีด + ย่อขนาดลง 62% |

---

## สี่: ไทม์ไลน์

ทุกทวีนเขียนเป็น `at(เลขเฟรม)` / `dur(f0, f1)` อ้างเฟรมของคลิปตรง ๆ
มีตัวคูณ `SLOW` ตัวเดียวคุมความเร็วทั้งไทม์ไลน์ (ตอนนี้ `1.5` → ยาว 9.45s
ตั้งเป็น `1` ถ้าอยากตรงคลิปเป๊ะ)

**ห้ามใส่จังหวะที่แต่งขึ้นเอง** เคยลองใส่ `back.in` ตอนยุบ, `expo.out` ตอนเบรก,
แยกการหมุนเป็นสามท่อน แล้วโดนตีกลับหมด — ทุกจังหวะต้องอ้างเฟรมได้

องก์แรก (f001-f053) เป็น DOM ล้วน อยู่ใน `.intro-orbit`:
ทุกชิ้นอยู่บน **วงโคจรเดียวกันตั้งแต่เฟรมแรก** ชิ้นใหญ่แค่อยู่วงนอกจนล้นเฟรม
รัศมีอยู่ในตัวแปร CSS `--r` ต่อชิ้น (ไม่ใช่ `scale` — ในคลิปขนาดตัวอักษรคงที่
มีแต่วงที่แคบเข้า) ตัวอักษรหมุนสวนด้วยค่าและ ease เดียวกันเป๊ะจึงตั้งตรงตลอด

---

## ห้า: โลโก้ J O E

ยกพาธมาจาก `JoeLettersSplash.tsx` ตรง ๆ ใช้พิกัด viewBox เดียวกัน
(`-10 -10 259 104`, หมึกกินช่วง x 0..239, y 0..84)

- **J** ตัวถังก้นมน + ลิ้นตรงกลาง + บากมุมซ้ายบน
- **O** กลีบวงรีสี่กลีบ
- **E** ตัวถัง + เดือยหน้า + ช่องกลางมุมมน + ช่องเปิดล่าง

### ประกอบร่างทีละชิ้น

ยกสัดส่วนจังหวะมาจากไทม์ไลน์ของสปแลช (ยาว 3 วินาที) แปลงเป็นช่วงบน `build` 0-1
ลำดับที่โผล่จึงเป็น **E → O → J**

| ชิ้น | ท่า | ช่วง `build` |
| --- | --- | --- |
| E ตัวถัง | `clip` กว้างขึ้น 4 → 77 | 0 – .44 |
| E ช่องกลาง | ยืดจากซ้าย | .13 – .35 |
| E เดือยหน้า | ยืดจากขอบขวา | .22 – .35 |
| E ช่องเปิดล่าง | ไถลเข้ามาจากขวา | .15 – .42 |
| O จุดนำ | วงกลม r 1→20 ที่กลางตัว O | .37 – .54 |
| O สี่กลีบ | แยกจากจุดกลางไปตำแหน่งจริง | .44 – .68 |
| O หมุนกลีบ | 0 → 59.4° / 28.6° | .47 – .94 |
| J ตัวถัง | ไหลลงมาจากบน y −100→0 | .29 – .87 |
| J บากมุม | ขยายจากศูนย์ | .48 – .96 |
| J ลิ้น | ขยายจากศูนย์ | .53 – 1.0 |

### เนื้อในถมทีละชิ้น

```
fill_i = max( ease( clamp01( (p_i − 0.45) / 0.55 ) ), solid )
```

ชิ้นเริ่มติดสีตอนเดินทางไปได้ราวครึ่งทางของท่าเข้าตัวเอง แล้วเต็มพอดีตอนเข้าที่
ชิ้นที่ออกตัวก่อนจึงติดสีก่อน — ประกอบไปติดสีไปแบบเหลื่อมกัน ไม่ใช่พร้อมกันเป๊ะ

---

## หก: กับดักที่เคยเดินชนมาแล้ว

อ่านทุกข้อก่อนแก้ ทุกข้อคือบั๊กที่เกิดขึ้นจริงและเสียเวลาไปกับมันแล้ว

1. **ห้ามเจาะช่องว่างก่อนลากเส้นขอบ** เส้นขอบจะไปถมช่องว่างคืน
   ช่องกลาง E กับบากมุม J หายไป โลโก้อ่านเป็น `O W B`

2. **ต้องคืน `globalAlpha = 1` ก่อนเจาะ** ค่าที่ค้างจากเส้นขอบเป็น 0 ตอน `solid = 1`
   ช่องว่างทั้งหมดไม่ถูกเจาะ โลโก้กลายเป็นก้อนตัน

3. **มาสก์ "ร้อน" ต้องผูกกับ `fill` ไม่ใช่ "ชิ้นที่กำลังเคลื่อน"**
   ถ้าผูกกับการเคลื่อน พอทุกชิ้นเข้าที่แล้วมาสก์ว่าง ทั้งโลโก้ค้างเป็นตัวอักษร

4. **รัศมีคอนทัวร์ต้องใหญ่ตอนต้น** เคยตั้งกลับด้าน (`0.5 + 0.5 × build`)
   ก้อนที่กำลังก่อตัวเลยเป็นเส้นบางแทบมองไม่เห็น

5. **เส้นขอบ 4 หน่วยบางเกินไป** พอย่อมาที่สเกลของตารางเหลือไม่ถึงหนึ่งช่อง
   จมหายไปกับการเบลอ ใช้ 10 (ราวสามช่อง)

6. **`guide` ต้องคูณเฉพาะค่าที่รอดจากการตัดแล้ว** ถ้าคูณทั้งผืน ทุกช่องถูกยกขึ้น
   ชั้นคราม ได้สนามจุดครามเต็มจอ — เป็นสิ่งที่ทำให้ภาพรวมดูไม่เหมือนคลิปมากที่สุด

7. **`spin` ห้ามหมุนเต็มรอบ** โลโก้จะแบนเป็นเส้นตรงกลางทางจนอ่านไม่ออก

8. **ห้ามใช้ state ใน render loop** `drive` เป็น ref เท่านั้น

9. **ปื้นขาวต้องล็อกไว้จนถึง f150** ไม่งั้นมันไปเกาะหัวที่กำลังก่อตัว
   ทำให้หัวเป็นสีขาวแทนที่จะเป็นส้ม

---

## เจ็ด: วิธีดีบัก

แผงดีบักมุมล่างซ้าย (dev เท่านั้น) มี:

- ปุ่ม play / pause / 0.1× / 0.25× / 1×
- แถบ scrub เลื่อนไปวินาทีไหนก็ได้
- ปุ่มกระโดดตามองก์ — `chaos / hold / implode / tile / heat / seed / solid / sweep / done`
- `hold` กันไม่ให้อินโทรปิดตัวเองระหว่างส่อง
- ค่าปัจจุบันของ `drive` ทุกฟิลด์

การสลับตัวอักษรในวงแหวนกำหนดตายตัวจากดัชนี ไม่สุ่ม — เลื่อน scrub กลับไปแล้ว
ได้ภาพเดิมเสมอ

### เทียบกับคลิปอ้างอิง

อย่าตัดสินด้วยการมองภาพของตัวเองอย่างเดียว วิธีที่ได้ผลคือจับภาพที่เวลาตรงกับ
เฟรมอ้างอิง (`t = f / 30 × SLOW`) แล้ววางเทียบข้างกัน ปัญหาที่หาไม่เจอมาหลายรอบ
โผล่ออกมาทันทีที่เห็นสองภาพติดกัน

/**
 * ริบบิ้นของฉากในหน้าต่าง ที่ "วิ่งได้จริง"
 *
 * ตัวละครช่วงที่ยังอยู่หลังกรอบต้องไถลบนผิวใบนี้ ไม่ใช่เส้นหลัก (ซึ่งเริ่มที่ปากบานที่ 2
 * เท่านั้น) — PortalRibbon เป็นคนลงทะเบียน mesh กับเส้นไว้ที่นี่ แล้ว Entrance หยิบไปใช้
 *
 * แยกเป็นโมดูลของตัวเองเพื่อไม่ให้ Entrance กับ NewHeroScene import วนกันเอง
 */
export const portalRide = {
  /** mesh จริงในฉาก — อ่านเมทริกซ์โลกจากตัวนี้ ไม่ต้องไล่คูณกลุ่มแม่เอง */
  mesh: null,
  curve: null,
  wave: 0,
  waves: 1,
}

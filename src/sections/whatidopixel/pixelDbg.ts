/**
 * ค่าจูนของจอ what-i-do (dev) — อ็อบเจกต์นิ่งใบเดียว ไม่ใช่ state
 *
 * ฉากอ่านค่าพวกนี้ใน useFrame แผงจูนยก state ของตัวเอง ฉากจึงไม่ถูก reconcile ใหม่ทุกครั้ง
 * ที่ลากสไลเดอร์ (บั๊กแบบเดียวกับที่เจอในแผงพอร์ทัลของจอ Experiences: ค่าที่ต้องปั้นของใหม่
 * ต้องทำในลูปเฟรม ไม่ใช่ผูกกับ useMemo ที่ไม่มีใครสั่งให้รันใหม่)
 *
 * ค่าที่ deploy ใช้คือค่าเริ่มต้นในไฟล์นี้เสมอ — แผงมีเฉพาะ import.meta.env.DEV
 */
export const pixelDbg = {
  /** ขนาดพอร์ทัลเทียบค่าที่ออกแบบไว้ (1 = ตามแบบ) */
  scale: 1,
  /**
   * หมุนตัวละคร — **องศา** ไม่ใช่เรเดียน (ลากสไลเดอร์ทีละ 1 องศาอ่านออกกว่าทศนิยมเรเดียน)
   *
   * rotY = หันซ้าย/ขวา เลือกว่าจะเห็นหน้าหรือด้านข้าง · rotX = ก้ม/เงย · rotZ = เอียงข้าง
   */
  rotY: 40,
  rotX: 0,
  rotZ: 0,
  /** ระยะกล้องถึงตัวละคร (หน่วยฉาก) */
  cam: 21.6,
  /**
   * ขนาดของตัวละครจริง (คูณกับสเกลของริก) — ไม่ใช่ "ซูมในหน้าต่าง" อีกแล้ว
   *
   * ภาพในพอร์ทัลคือภาพหน้าจอที่ทับตัวจริงพอดี (ดูหัวไฟล์ PixelStage) หน้าต่างจึงไม่มีซูม
   * ของตัวเอง อยากให้ตัวใหญ่ในพอร์ทัลก็ขยายตัวจริง
   */
  zoom: 1.12,
  /** เลื่อนตัวละครขึ้น/ลงตามแกนกล้อง (หน่วยฉาก) — หน้าต่างอยู่นิ่ง ตัวเลื่อนเข้า/ออกจากมัน */
  lift: 0.15,
  /**
   * เลื่อนตัวละครซ้าย/ขวาตามแกนขวาของกล้อง (หน่วยฉาก)
   *
   * ลบมาก ๆ = ตัวเลื่อนไปพ้นขอบซ้ายของหน้าต่าง ส่วนที่พ้นออกไปวาดเป็น 3D จริง (ดู spill)
   * ไปตามแกนกล้อง ไม่ใช่แกน X ของโลก เพราะกล้องอยู่เฉียง ลากตามแกนโลกจะเดินเฉียงบนจอ
   */
  shiftX: 0,
  /**
   * ขยับตัวพอร์ทัลบนจอ (หน่วยฉาก) — x = ซ้าย/ขวา · y = ขึ้น/ลง ตามแกนของกล้อง
   *
   * ต่างจาก lift/shiftX ที่ขยับ *ตัวละครเทียบกับหน้าต่าง*: สองค่านี้ย้าย *หน้าต่าง* ไปทั้งใบ
   * พร้อมภาพข้างใน ใช้จัดที่วางพอร์ทัลในเลย์เอาต์ของจอ
   */
  posX: 0,
  posY: 0,
  /**
   * ให้ตัวละคร 3D จริงยื่นออกจากขอบซ้ายของพอร์ทัลได้แค่ไหน (หน่วยฉาก)
   *
   * 0 = ตัดตรงขอบซ้ายพอดี เห็น 3D เฉพาะส่วนที่เลยขอบออกไป (ข้างในยังเป็นบล็อกพิกเซล)
   * บวก = เลื่อนรอยตัดเข้าไปข้างในพอร์ทัล เห็นตัวจริงทับบล็อกมากขึ้น
   * ลบ = ตัดนอกขอบ เหลือส่วนที่ยื่นออกมาน้อยลง
   */
  spill: 0,
  /** เวลาทั้งหมดที่ไล่บล็อกจากกล่องหลักถึงใบสุดท้าย (วินาที) */
  stagger: 0.85,
  /** เวลาที่บล็อกหนึ่งใบใช้ยื่นออก (วินาที) */
  grow: 0.3,
}

/**
 * ค่าเริ่มต้นถ่ายไว้ตอนโหลดโมดูล — ปุ่ม "คืนค่าเริ่มต้น" กับจุดสีเขียวที่บอกว่าแถวไหนถูกแก้
 * ต้องเทียบกับของจริงในไฟล์นี้ ไม่ใช่เลขที่พิมพ์ซ้ำไว้ในแผง (พิมพ์ซ้ำ = วันหนึ่งไม่ตรงกัน)
 */
export const PIXEL_DEFAULTS: Readonly<typeof pixelDbg> = { ...pixelDbg }

/** ค่าที่ฉากเขียนกลับให้แผงอ่าน (อ่านอย่างเดียว) */
export const pixelRead = {
  t: 0,
  /** ความสูงพอร์ทัลบนจอ (พิกเซล) เทียบกับความสูงของภาพที่ถ่าย — บอกว่าภาพพอหรือไม่ */
  onScreen: 0,
  rt: 0,
}

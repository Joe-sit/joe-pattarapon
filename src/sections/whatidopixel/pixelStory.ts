/**
 * สกิลที่วางรอบก้อนพิกเซล — ข้อความมาจาก sections/whatido ที่เดียว ที่นี่เพิ่มแค่ "ของ" กับ "ที่วาง"
 *
 * pick เป็นอ็อบเจกต์นิ่ง ไม่ใช่ state: ตัว section เขียนลงมันตอน hover แล้วฉากอ่านใน useFrame
 * ถ้าส่งเป็น prop ฉากทั้งฉากจะ reconcile ใหม่ทุกครั้งที่เมาส์เข้า-ออกของแต่ละชิ้น
 */
export const pick: { at: number | null } = { at: null }

/** ชนิดของที่ใช้แทนสกิลนั้น (ดู PROP_GEO ใน PixelStage) */
export type PropKind = 'ring' | 'blob' | 'arrow'

/**
 * ที่วางของแต่ละชิ้นในพิกัดฉาก + ที่วางป้ายในพิกัดจอ
 *
 * ในภาพอ้างอิงของกระจายรอบก้อนแบบไม่สมมาตร: วงโดนัทบนซ้าย ดอกไม้ล่างขวา ลูกศรบนขวา
 * ตัวเลขข้างล่างวางตามผังนั้น (y ราวระดับหัว/เอวของตัวละครซึ่งยืนอยู่ที่ y ≈ 2.6)
 */
/**
 * ที่วางของแต่ละชิ้น (พิกัดฉาก) + ที่วางป้าย (พิกัดจอ)
 *
 * คิดจากของจริง ไม่ใช่กะ: วัดริกของ hero ได้รัศมี 3.63 หน่วย จุดกลาง (−0.56, −0.40, −0.42)
 * กล้องมองเข้าหาจุดนั้นจากทิศ (0.76, 0.27, 0.57) ระยะ 21.6 หน่วย (fov 30 → เห็นสูงราว
 * 11.6 หน่วย = สองเท่าของตัว เหลือที่ว่างรอบให้ของ) ทิศ "ขวาของจอ" ในพิกัดโลกจึงเป็น
 * (0.57, 0, −0.76) — ค่านี้วัดจากภาพที่ถ่ายได้ ไม่ใช่จากการคูณเวกเตอร์ในหัว (คำนวณครั้งแรก
 * ได้เครื่องหมายกลับด้าน ของทั้งสามชิ้นเลยไปโผล่คนละฝั่งกับป้ายของตัวเอง) "บนของจอ" คือ +Y
 * เกือบตรง ตัวเลขข้างล่าง = จุดกลาง + ผลรวมของสองทิศนั้น
 *
 * ป้ายอยู่มุมเฟรม ของอยู่ริมกลาง — รอบก่อนวางไว้มุมเดียวกันทั้งคู่ ป้าย Design เลยทับดอกไม้
 */
export const SLOTS: { kind: PropKind; at: [number, number, number]; label: string }[] = [
  {
    kind: 'ring',
    at: [-4.44, -0.2, 4.75],
    label: 'left-[clamp(24px,7vw,110px)] top-[clamp(96px,18svh,190px)]',
  },
  {
    kind: 'blob',
    at: [2.4, -4.6, -3.53],
    label: 'right-[clamp(24px,7vw,110px)] bottom-[clamp(88px,16svh,170px)] text-right',
  },
  {
    kind: 'arrow',
    at: [3.32, 0.2, -5.59],
    label: 'right-[clamp(24px,7vw,110px)] top-[clamp(96px,18svh,190px)] text-right',
  },
]

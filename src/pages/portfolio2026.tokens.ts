/**
 * โทเคนหน้าตาของหน้า /2026 ที่ถูกใช้ข้ามไฟล์
 *
 * แยกออกมาเพราะ section "What I Do" ย้ายไปเป็นคอมโพเนนต์ร่วม (ใช้ทั้ง /2026 และ /2026-final)
 * แล้วยังต้องแต่งตัวด้วยชุดเดียวกับหน้าเดิม — ก๊อบสตริงไปไว้สองที่คือของที่จะเพี้ยนจากกันวันหนึ่ง
 */

export const BORDER = 'border border-[var(--v2-line)]'
export const CELL = `${BORDER} bg-[var(--v2-bg)]`
export const PAD = 'p-[clamp(1rem,1.67vw,2rem)]'
export const BODY_TEXT = 'text-[clamp(0.9375rem,1.05vw,1.375rem)]'
